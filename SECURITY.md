# 安全政策

## 支持范围

| 版本 | 状态 |
|---|---|
| 0.3.x | 当前支持 |
| 0.2.x 及更早版本 | 不再提供安全修复 |

项目处于 Alpha 阶段。接口兼容性可能变化，但凭据保护、认证边界、文件安全和数据完整性问题会优先处理。

## 私下报告

请使用 GitHub 仓库的 **Security → Report a vulnerability** 提交私密报告：

<https://github.com/A11MiND/bilibili_crawler_cli/security/advisories/new>

不要在公开 Issue、Discussion、PR 或日志中披露漏洞细节。

报告应包含最小复现步骤、受影响版本、预期影响和已进行的脱敏处理。请勿提交：

- Cookie、令牌或账号信息。
- 真实 CSV、state、评论、用户 ID、IP 属地或完整 API 响应。
- 本机绝对路径或其他个人信息。

如 Cookie 可能已泄露，应立即退出相关会话或更新凭据，不要等待项目回复。

## 安全问题范围

包括但不限于：

- Cookie 泄露、认证静默降级或不安全重定向。
- 路径穿越、任意文件覆盖或命令注入。
- CSV 公式注入防护失效。
- checkpoint 损坏导致错误覆盖或数据混写。
- 可能绕过项目既定访问控制边界的实现缺陷。

上游接口变更、正常限流、公开内容可见性差异和规避平台风控的请求不属于安全漏洞。

维护者会通过 GitHub 私密渠道确认、评估和协调修复。项目不承诺固定响应时限。
